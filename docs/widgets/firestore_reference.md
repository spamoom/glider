# Firestore Reference Widget

An auto-updating widget based on a definition stored in Cloud Firestore.

```json
{
    "type": "firestore_ref",
    "ref": "collection/document1"
}
```

The document stored at `ref` should be a valid Cocoon definition.